/*
 * @Descripttion:换肤js入口文件
 * @version: 1.0
 * @Author: gqd
 */

// import { theme as dark } from './ui-dark';
import { theme as light } from './ui-light';
// import setting from '../defaultSettings';

// export const theme = setting.theme === 'light' ? light : dark;
export const theme = light;
